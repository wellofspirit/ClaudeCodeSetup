{
  "name": "bundle-analyzer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "bun build --compile cli.mjs --outfile bundle-analyzer",
    "install": "./install.sh"
  },
  "devDependencies": {
    "@swc/core": "^1.11.24"
  },
  "trustedDependencies": [
    "@swc/core"
  ]
}
